
// TODO: Define the `Book` struct with public fields `title`, `author` and `isbn`. You should use the
//       `String` type for simplicity for these fields.
//
// Derive/Implement the traits and methods necessary for exercise codes to run without panicking.

pub struct Book {}
