//use std::collections::HashMap;
//use crate::book::Book;

// TODO: Define the `Library` struct with public fields `books` vector and `borrowed` mapping
//       ISBN->User of type `String` for simplicity to represent the borrowed books.
//
// Derive/Implement the traits and methods necessary for exercise codes to run without panicking.


pub struct Library {}
